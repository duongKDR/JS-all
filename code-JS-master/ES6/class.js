class count {
    constructor( name, so){
        this.name = name;
        this.so = so;

    }
    getName(){
        return this.name
    }
}
 const nameK = new count("K",1);
 const nameM = new count("M",2);
 console.log(nameK);